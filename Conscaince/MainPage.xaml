<Page
    x:Class="Conscaince.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Conscaince"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <RelativePanel VerticalAlignment="Center" HorizontalAlignment="Stretch" >
            <TextBlock x:Name="debugTitle" Text="" FontSize="30" FontFamily="Noto Nastaliq Urdu" Padding="40" Foreground="LightGray" RelativePanel.AlignHorizontalCenterWithPanel="True" HorizontalAlignment="Center"/>
            <Button x:Name="beginButton" Content="Begin" Click="BeginButton_Click" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="debugTitle" HorizontalAlignment="Center" Background="LightGray" />
            <Button x:Name="yesButton" Content="Yes" Click="YesButton_Click" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="beginButton" HorizontalAlignment="Center" Background="LightGray" />
            <Button x:Name="noButton" Content="No" Click="NoButton_Click" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="beginButton" RelativePanel.RightOf="yesButton" HorizontalAlignment="Center" Background="LightGray" />
        </RelativePanel>
    </Grid>
</Page>
